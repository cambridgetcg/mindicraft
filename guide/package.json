{
  "name": "mindicraft-guide",
  "private": true,
  "type": "module",
  "description": "The zero-to-one guide of human civilisation — static site builder",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test ../test/*.test.mjs",
    "verify": "npm test && npm run build && node ../test/verify-castle-build.mjs",
    "castle:sync": "npm test && node ../castle-to-mindicraft.mjs --write && node ../castle-to-mindicraft.mjs --check && npm run build && node ../test/verify-castle-build.mjs"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
